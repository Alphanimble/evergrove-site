// Server component — delegate all interactive UI to the client `HomeClient` component
import HomeClient from '@/components/pages/HomeClient'

export default function Page() {
  return <HomeClient />
}
